name: prsplit close original #73

on:
  pull_request:
    types: [closed]

permissions:
  pull-requests: write
  contents: write

jobs:
  close-original-pr:
    if: ${{ github.event.pull_request.merged == true && github.event.pull_request.number == 77 }}
    runs-on: ubuntu-latest
    steps:
      - name: Close original PR
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: 73,
              state: 'closed',
            });

            const originalBaseBranch = "main";
            const cleanupWorkflowFilenames = ["chain-74-to-75.yml","chain-75-to-76.yml","chain-76-to-77.yml","close-original-73.yml"];
            console.log(
              `Cleaning up prsplit workflows for base "${originalBaseBranch}"...`
            );

            for (const workflowFilename of cleanupWorkflowFilenames) {
              const workflowPath = `.github/workflows/${workflowFilename}`;

              try {
                const { data: workflowFile } = await github.rest.repos.getContent({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  path: workflowPath,
                });

                if (Array.isArray(workflowFile) || !('sha' in workflowFile)) {
                  continue;
                }

                await github.rest.repos.deleteFile({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  path: workflowPath,
                  message: `chore: remove prsplit workflow ${workflowFilename}`,
                  sha: workflowFile.sha,
                });
              } catch (error) {
                if (error && typeof error === 'object' && 'status' in error && error.status === 404) {
                  continue;
                }
                throw error;
              }
            }